## What this changes

<!-- The problem, then the fix. If it fixes an issue, "Fixes #123". -->

## How it was verified

<!-- `npm run check` is the floor. For anything touching a source adapter, a
     rendered page, or a Plaid link token, say what you actually exercised —
     these are the places tests alone do not catch. -->

- [ ] `npm run check` passes (typecheck + tests)
- [ ] Docs updated in this commit (`README.md` / `CLAUDE.md`), per CLAUDE.md rule 13
- [ ] No real balance, account number, token or personal detail in the diff —
      including test fixtures

## Anything reviewers should push back on

<!-- Trade-offs you made, things you were unsure about. Optional, and useful. -->
